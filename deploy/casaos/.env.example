# CasaOS / docker-compose environment file for Syllogic
#
# Usage:
# - Copy to `.env` in the same folder as `docker-compose.yml`
# - Update values (especially secrets)
# - Start via CasaOS UI or `docker compose up -d`
#
# NOTE: CasaOS is typically accessed via LAN IP; set APP_URL accordingly.

# Image channel policy:
# - edge: development/testing only
# - vX.Y.Z: production pinning
APP_VERSION=edge

# Where you access the app from your browser
APP_URL=http://localhost:8080

# CasaOS will set WEBUI_PORT automatically (leave blank unless running manually)
WEBUI_PORT=8080

# MCP external port (internal MCP port is fixed at 8001)
MCP_PORT=8001

# CasaOS system envs (UID/GID/TZ). Defaults used if not provided.
PUID=1000
PGID=1000
TZ=UTC

# REQUIRED: change this to a long random secret (at least 32 chars)
BETTER_AUTH_SECRET=change-me

# REQUIRED: shared secret for app -> backend signed internal auth
INTERNAL_AUTH_SECRET=change-me

# Recommended in production: app-layer encryption keys
# DATA_ENCRYPTION_KEY_CURRENT=
# DATA_ENCRYPTION_KEY_PREVIOUS=
# DATA_ENCRYPTION_KEY_ID=k1

# Postgres credentials (REQUIRED: change POSTGRES_PASSWORD)
POSTGRES_USER=financeuser
POSTGRES_PASSWORD=change-me
POSTGRES_DB=finance_db

# Optional: Backend CORS allowlist (comma-separated). Defaults to APP_URL.
# CORS_ALLOW_ORIGINS=http://localhost:8080

# Optional: External integrations
OPENAI_API_KEY=
